extends layout

include mixins/_burstCard

block content
  .inner
    h1 #{user.name}'s bursts
    .card-wrapper.flex-container-row.justify-space-between.flex-wrap
      //- pre= h.dump(user)
      each burst in bursts
        +burstCard(burst, user)
  
